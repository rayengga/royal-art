# Database (Neon / Postgres)
# Use ONLY the connection string (not the full `psql ...` command).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# JWT Secret (generate a secure random string for production)
JWT_SECRET="replace-with-a-strong-32+-char-secret"

# Next.js
# Optional: set this only if you need an absolute API base URL in the browser.
# NEXT_PUBLIC_API_URL="http://localhost:3000"

# Email Configuration (SMTP)
# For Gmail: 
# 1. Enable 2-Step Verification in your Google Account
# 2. Go to: Google Account -> Security -> 2-Step Verification -> App passwords
# 3. Generate an App Password and use it below (not your regular Gmail password)
# 4. Replace SMTP_USER with your Gmail address
# 5. Replace SMTP_PASS with the generated App Password

SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-16-char-app-password"
ORDER_NOTIFICATION_EMAIL="your-notification-email@example.com"

# Note: For other email providers (Outlook, Yahoo, etc.):
# - Update SMTP_HOST, SMTP_PORT accordingly
# - SMTP_SECURE="true" for port 465, "false" for port 587
